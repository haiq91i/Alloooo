[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "poppler_utils", "zbar", "libGL"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
